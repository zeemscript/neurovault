{
  "name": "neurovault-extension",
  "version": "0.1.0",
  "private": true,
  "description": "NeuroVault - AI Security Monitor Chrome Extension",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.0"
  }
}
